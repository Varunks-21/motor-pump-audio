# Motor Pump Audio Event Detection - Phase 1
# All packages install cleanly on Windows with plain `pip install -r requirements.txt`.
# `torch` resolves to the CPU build by default on Windows (no CUDA needed).

numpy>=1.24
scipy>=1.10
librosa>=0.10.1
soundfile>=0.12.1
torch>=2.1
matplotlib>=3.7
flask>=3.0
flask-socketio>=5.3
simple-websocket>=1.0
sounddevice>=0.4.6